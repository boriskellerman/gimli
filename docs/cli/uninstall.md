---
summary: "CLI reference for `gimli uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
---

# `gimli uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
gimli uninstall
gimli uninstall --all --yes
gimli uninstall --dry-run
```

